<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 临时密码一次性登录查询；所有变量使用#{...}预编译绑定，明文不会进入本Mapper。 -->
<mapper namespace="com.hyf.agent_work_foot.admin.mapper.TemporaryPasswordMapper">
    <!-- 作用：锁定最近生成的临时密码；输入：userId；输出：哈希与生命周期。 -->
    <select id="selectLatestForUpdate" resultType="com.hyf.agent_work_foot.admin.mapper.TemporaryPasswordMapper$TemporaryPasswordRow">
        SELECT id,
               user_id AS userId,
               password_hash AS passwordHash,
               expires_at AS expiresAt,
               used_at AS usedAt,
               revoked_at AS revokedAt
        FROM temporary_passwords
        WHERE user_id = #{userId}
        ORDER BY created_at DESC, id DESC
        LIMIT 1
        FOR UPDATE
    </select>

    <!-- 作用：严格一次消费；输入：记录和UTC时间；输出：影响行数。 -->
    <update id="markUsed">
        UPDATE temporary_passwords
        SET used_at = #{usedAt}
        WHERE id = #{id}
          AND used_at IS NULL
          AND revoked_at IS NULL
          AND expires_at &gt; #{usedAt}
    </update>
</mapper>
