<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 管理员审计写入；所有变量使用#{...}预编译绑定，detailJson只允许非敏感摘要。 -->
<mapper namespace="com.hyf.agent_work_foot.admin.mapper.AdminAuditMapper">
    <!-- 作用：写入审计事件；输入：audit；输出：影响一行。 -->
    <insert id="insertAudit">
        INSERT INTO admin_audit_logs (
            id, admin_user_id, target_user_id, action, result, request_id, detail_json, created_at
        ) VALUES (
            #{audit.id}, #{audit.adminUserId}, #{audit.targetUserId}, #{audit.action}, #{audit.result},
            #{audit.requestId}, CAST(#{audit.detailJson} AS JSON), #{audit.createdAt}
        )
    </insert>

    <!-- 作用：核验目标动作审计；输入：targetUserId、action；输出：数量。 -->
    <select id="countByTargetAndAction" resultType="long">
        SELECT COUNT(*)
        FROM admin_audit_logs
        WHERE target_user_id = #{targetUserId}
          AND action = #{action}
    </select>

    <sql id="auditLogFilters">
        <where>
            <if test="query.adminAccount != null">
                admin_user.admin_login_name LIKE CONCAT('%', #{query.adminAccount}, '%') ESCAPE '\\'
            </if>
            <if test="query.targetUserEmail != null">
                AND target_user.email LIKE CONCAT('%', #{query.targetUserEmail}, '%') ESCAPE '\\'
            </if>
            <if test="query.targetUserNickname != null">
                AND target_user.nickname LIKE CONCAT('%', #{query.targetUserNickname}, '%') ESCAPE '\\'
            </if>
            <if test="query.action != null">
                AND admin_log.action = #{query.action}
            </if>
            <if test="query.result != null">
                AND admin_log.result = #{query.result}
            </if>
            AND admin_log.created_at &gt;= #{query.startAt}
            AND admin_log.created_at &lt; #{query.endAt}
        </where>
    </sql>

    <!-- 作用：读取管理员审计页；账号识别字段仅来自users，不读取用户业务表。 -->
    <select id="selectAuditLogPage" resultType="com.hyf.agent_work_foot.admin.mapper.AdminAuditMapper$AdminAuditLogRow">
        SELECT admin_log.id,
               admin_user.admin_login_name AS adminAccount,
               admin_user.nickname AS adminNickname,
               admin_log.target_user_id AS targetUserId,
               target_user.email AS targetEmail,
               target_user.nickname AS targetNickname,
               admin_log.action,
               admin_log.result,
               admin_log.request_id AS requestId,
               admin_log.detail_json AS detailJson,
               admin_log.created_at AS createdAt
        FROM admin_audit_logs admin_log
        LEFT JOIN users admin_user ON admin_user.id = admin_log.admin_user_id
        LEFT JOIN users target_user ON target_user.id = admin_log.target_user_id
        <include refid="auditLogFilters"/>
        ORDER BY admin_log.created_at DESC, admin_log.id DESC
        LIMIT #{query.size} OFFSET #{query.offset}
    </select>

    <!-- 作用：统计审计筛选页总数；与列表使用完全相同的过滤条件。 -->
    <select id="countAuditLogs" resultType="long">
        SELECT COUNT(*)
        FROM admin_audit_logs admin_log
        LEFT JOIN users admin_user ON admin_user.id = admin_log.admin_user_id
        LEFT JOIN users target_user ON target_user.id = admin_log.target_user_id
        <include refid="auditLogFilters"/>
    </select>
</mapper>
